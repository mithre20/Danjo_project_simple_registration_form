services:
  - type: web
    name: danjo-registration
    env: python
    buildCommand: pip install -r requirements.txt && python manage.py collectstatic --no-input --clear || true
    startCommand: gunicorn userauth_project.wsgi:application
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: False
